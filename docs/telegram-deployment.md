# Telegram deployment

Telegram setup is now part of the local web interface. Start the single app with
`docker compose up -d --build`, open `http://localhost:8000`, and choose **Telegram**.

See [the current self-hosting guide](self-hosting.md) for pairing, phone access,
server operation, and migration from the older worker-only Compose configuration.
