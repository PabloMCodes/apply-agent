# Optional private hostname used when accessing the installation from another device.
# Keep the app behind a private network or SSH tunnel; it has no public login.
APPLY_AGENT_HOSTS=
# Telegram is configured in the local web interface. No token is required here.
